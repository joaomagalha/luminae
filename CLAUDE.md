# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

Luminae is in its initial stage. The only source file is `index.html` (currently empty). No build tooling, framework, or test suite has been set up yet.

## Stack

Currently a static HTML project. The `.gitignore` anticipates optional future Node.js tooling (`node_modules`, `dist/`, `build/`, `.cache/`), but nothing has been added yet.

## Development

Open `index.html` directly in a browser — no build step required at this stage.

If/when a Node.js toolchain is introduced, update this file with the relevant `install`, `dev`, `build`, and `test` commands.
