Google Fonts (Inter) — web font provider used for general typography
Vanilla CSS — custom styling, grid alignments, spacing variables, and responsiveness
Vanilla JavaScript — toggles mobile menu classes and handles navbar scrolling states
IntersectionObserver API — scroll-triggered animations to fade in layout blocks
